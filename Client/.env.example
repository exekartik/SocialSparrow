# Local development only — set this for your local backend URL
# Leave blank or omit in Vercel deployment (Vercel routes /api requests automatically via vercel.json)
VITE_API_BASE_URL=http://localhost:3000/api
